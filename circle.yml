test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/reports
    - ava --tap | tap-mocha-reporter xunit > $CIRCLE_TEST_REPORTS/reports/xunit.xml
    - ava --tap | tap-xunit > $CIRCLE_TEST_REPORTS/reports/xunit.xml
    - eslint --format=node_modules/eslint-tap src/
    - npm test
